- name: Condition in ansible
  hosts: local
  connections: local
  vars:
    Name: Hotstar
    IS_LIVE: true
  tasks:
  - name: checking is live or not
    ansible.buitin.debug:
      msg: "{{ name }} yes it is live"
    when: IS_LIVE